<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guoqiang.rxddataserver.entity.User">
    <select id="findById" parameterType="string" resultType="com.guoqiang.rxddataserver.entity.User">
        SELECT * FROM User WHERE id=#{id}
    </select>
</mapper>